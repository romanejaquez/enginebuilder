import 'package:flutter/material.dart';

class NameNumberWidget extends StatelessWidget {
  const NameNumberWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('NameNumber')
    );
  }
}